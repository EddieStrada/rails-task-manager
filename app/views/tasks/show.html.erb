<h1><%= @task.title %></h1>

<%= link_to "Edit this Task", edit_task_path %>

<p><%= @task.details %></p>

<p>
  <% if @task.completed? %>
    <%= p '☑️ this task is completed' %>
  <% else %>
    <%= p '🔲 this task is not completed yet' %>
<% end %></p>

<br>
<%= link_to "Back to list", tasks_path %>
