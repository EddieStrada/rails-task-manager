<h1>Create a New Task</h1>

<!-- <form action="/tasks" method="post">
  <input type="text" name="name">
</form> -->
<!-- task.new(name: params[:name], address: params[:adddress])
task.new(params[:task])
params[:task] = {
  name: "value",
  address: 'value'
} -->

<!-- FORM FOR:
1. Building our path/action
2. Setting the method
3. Adding authenticity
4. Adds class & id -->

<% if @task.errors.any? %>
  <% @task.errors.full_messages.each do |msg| %>
    <p><%= msg %></p>
  <% end %>
<% end %>

<!-- @task does not have an id! -->
<%= form_for @task do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :details %>
  <%= f.text_field :details %>
  <%= f.submit %>
<% end %>

<%= link_to "Back to list", tasks_path %>
